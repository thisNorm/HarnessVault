# 애플리케이션
NODE_ENV=development
PORT=3000

# 데이터베이스 (docker-compose.yml 기본값과 일치)
DATABASE_URL=postgresql://harness:harness@localhost:5432/harnessvault

# 세션 유효 기간 (시간)
SESSION_TTL_HOURS=720

# 웹 콘솔 origin (쉼표 구분)
WEB_ORIGINS=http://localhost:3100
